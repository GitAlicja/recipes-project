{{> navigationalBar}}
<div class="container">

  <h2 class="mb-3">Your Bookmarks</h2>

  <div class="row justify-content-center">
    <div class="col-12 col-md-10 col-lg-8">
      <form class="card card-sm border-light">
        <div class="card-body row no-gutters align-items-center card-backgroung">
          <div class="col-auto">
            <i class="fas fa-search h4 text-body"></i>
          </div>
          <div class="col mr-3">
            <form action="/user/bookmarks" method="GET">
              <input class="form-control form-control-lg form-control-borderless" type="text" name="query"
                placeholder="Browse my bookmarks" value="{{this.query}}">
          </div>
          <div class="col-auto">

            <button class="btn btn-lg btn-success" type="submit">Search</button>
          </div>
        </div>

      </form>
    </div>
  </div>


  <div class="recipes">
    <h2>Recipes</h2>
    <div class="row">
      <div class="wrapper">
        {{!-- {{#if this.recipes}} --}}
        {{#each this.user.bookmarkedRecipes}}
        <div class="card-deck col-lg-4 col-sm-12 col-md-5 text-center">
          <div class="card mt-4 pb-3 card-border">
            <img class="card-img-top"
              src="{{#if this.recipeImage}} {{this.recipeImage}}{{else}}/images/ella-olsson-7EhPbdAQG-s-unsplash.jpg{{/if}}"
              alt="Card image cap">
            <div class="card-body-recipes">
              <h5 class="card-title">{{this.name}}</h5>
              <a href="/recipes/{{this._id}}" class="btn btn-primary">Go to Recipe</a>
            </div>

          </div>
        </div>
        {{/each}}
      </div>

    </div>

  </div>

</div>

{{> footer}}



{{!-- <form action="/user/bookmarks" method="GET">
  <input type="text" name="query" placeholder="Browse my bookmarks" value="{{this.query}}">
  <button type="submit" id="search-button">Search</button>
</form> --}}


{{!-- 'this' is the user object --}}
{{!-- {{#each this.user.bookmarkedRecipes}}
<p>{{this.name}}</p>
<img src="{{this.recipeImage}}">
<p>Created by: {{this.createdBy.username}}</p>
<p><a href="/recipes/{{this._id}}">Go to Recipe</a></p>
{{/each}} --}}